syntax = "proto3";

package enums;

enum pComponentClientReturnType
{
    kSuccess = 0;
    kGeneralError = 1;
    kPending = 2;
    kInvalid = 3;
    kUnchanged = 4;
}

enum pStateUpdateMode
{
    kPoll = 0;
    kEvent = 1;
}

enum pApplicationState
{
    kInitializing = 0;
    kRunning = 1;
    kShuttingdown = 2;
    suspend = 3;
}